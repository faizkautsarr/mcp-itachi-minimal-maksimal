Run the FE Agent to validate code using the mcp-itachi server.

Call the `mcp__mcp-itachi__fe_validate` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
