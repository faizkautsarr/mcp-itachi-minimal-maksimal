Run the Architect Agent to review architecture using the mcp-itachi server.

Call the `mcp__mcp-itachi__architect_review` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
