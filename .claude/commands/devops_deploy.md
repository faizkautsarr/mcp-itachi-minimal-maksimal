Run the DevOps Agent to deploy using the mcp-itachi server.

Call the `mcp__mcp-itachi__devops_deploy` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
