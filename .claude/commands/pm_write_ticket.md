Run the PM Agent to write a ticket using the mcp-itachi server.

Call the `mcp__mcp-itachi__pm_write_ticket` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
