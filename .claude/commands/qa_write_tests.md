Run the QA Agent to write tests using the mcp-itachi server.

Call the `mcp__mcp-itachi__qa_write_tests` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
