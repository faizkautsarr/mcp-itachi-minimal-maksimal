Run the Designer Agent to create a design spec using the mcp-itachi server.

Call the `mcp__mcp-itachi__designer_create_design` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
