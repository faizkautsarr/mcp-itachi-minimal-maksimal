Run the PM Agent to create a product spec using the mcp-itachi server.

Call the `mcp__mcp-itachi__pm_create_spec` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
