Run the BE Agent to implement an API using the mcp-itachi server.

Call the `mcp__mcp-itachi__be_implement_api` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
