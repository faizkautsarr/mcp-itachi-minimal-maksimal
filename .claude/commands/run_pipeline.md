Run the full agentic pipeline (PM → Architect → Designer → BE → FE → QA → DevOps) using the mcp-itachi server.

Call the `mcp__mcp-itachi__run_pipeline` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
