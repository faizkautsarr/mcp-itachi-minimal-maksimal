Run the FE Agent to implement UI using the mcp-itachi server.

Call the `mcp__mcp-itachi__fe_implement` tool with `input` set to: $ARGUMENTS

After the tool returns, display the download URL to the user.
